<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database Connection Settings -->
        <!-- connection.url / connection.username / connection.password are intentionally
             omitted here and injected at runtime by HibernateUtil from the DB_URL,
             DB_USERNAME and DB_PASSWORD environment variables, so credentials never
             live in a file committed to source control. -->
        <property name="connection.driver_class">org.postgresql.Driver</property>

        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>
        <property name="hbm2ddl.auto">update</property>

        <mapping class="com.dairyfarm.model.Cattle"/>
        <mapping class="com.dairyfarm.model.MilkProduction"/>
    </session-factory>
</hibernate-configuration>